# 更新日志

## v1.4.0 — 1.4.0-rc.1

> 从「聊天客户端」到「能动手的 Agent」。本次把工具调用、工作区、MCP 三条链路全部打通，并重做了导航与附件系统。

### 工具调用

- **14 个内置工具**：`file_read` / `file_write` / `file_append` / `file_list` / `file_search` / `file_info` / `file_exists` / `file_mkdir` / `file_delete` / `file_move` / `file_copy` / `file_rename` / `execute_shell` / `interactive_clarification`
- **需求不明确会反问**：`interactive_clarification` 让模型在动手前先问你，而不是猜
- **逐工具审批**：每个工具可单独设为「自动放行」或「每次询问」，按工作区保存
- **过程可视化**：思考过程与工具调用按时间线展开，参数/结果各自可折叠
- **续写不新开气泡**：工具调用后的续写复用同一条助手消息，不再切出一条新消息
- **执行不卡界面**：工具在工作线程串行执行，主线程不再被 shell 阻塞

### 工作区

- **多工作区**：可创建多个互不影响的工作区，各自独立的文件目录与审批配置
- **每个助手绑定一个**：助手里单选绑定，切换助手即切换工作区
- **内置 proot 环境**：预编译 `libproot_exec.so` / `libproot_loader.so`（arm64-v8a / x86_64）
- **一键安装 rootfs**：默认 Ubuntu Base 24.04，**支持断点续传**（大文件中断后从断点接着下，自动重试 8 次）
- **文件浏览 + 终端**：内置文件管理器与命令输入
- **可跑 bash / git / python**：rootfs 内是完整 Ubuntu 用户态

### MCP

- **HTTP 与 SSE 双传输**：可在服务器配置里选择 `Streamable HTTP` 或 `SSE`
- **自定义请求头**：逐行编辑名称/值，值可切换明文，全部随请求发出
- **逐工具开关**：拉取到工具列表后，每个工具可单独启用/停用
- **逐工具审批**：每个工具可单独设为需人工确认
- **工具预览**：列表页直接显示工具名与描述，测试连接即可看到发现结果

### 统一导航

- **返回栈可恢复**：进程被回收后重建，返回栈完整还原
- **页面可注册内层返回**：子页面（详情/编辑）自己接管返回，不再错级
- **前进后退自动动画**：按返回栈层级判断方向
- **动画可按路由定制**：`AppNavRegistry` 全局换一套，或单路由覆盖
- **修复返回错级**：此前子页面返回会直接跳回设置首页

### 思考与缓存

- **思考等级五档**：自动 / 关闭 / 低 / 中 / 高
- **按对话生效**：设置记在当前对话上，切模型不丢、不影响其他对话
- **按厂商适配参数**：OpenRouter / 阿里云百炼 / 火山方舟 / 智谱 / DeepSeek / 硅基流动等各发各自的形状（一律发 `reasoning_effort` 会被服务端**静默忽略**，比报错更难查）
- **支持 Response API**：服务商级开关 + 对话级覆盖
- **Anthropic 提示缓存**：四个插入点（顶层 / system / tools / 倒数第二条 user 消息），可选 5 分钟或 1 小时 TTL
- **开了思考不带 temperature**：避免 o 系列 / DeepSeek 直接报错

### 附件系统

- **多选图片与文件**：一次选多个，图片与任意文件分开入口
- **复制到私有目录**：不再依赖 content:// 授权，重启后仍可读
- **SHA-256 内容去重**：同一文件经不同 picker 导入只保留一份
- **图片压缩对齐 rikkahub**：10000px / 16M 像素 / JPEG 85，在**编码时**压缩而非导入时
- **文本内联上限 512 KB**：超过则截断并提示
- **图片统一转 JPEG**：EXIF 方向归一后再压（GIF 不动，避免破坏动图）

### 其他

- 关于页新增**打赏入口**（扫码图可直接保存）
- 新增**打开应用私有目录**（跳系统文件管理器）
- 文档提供器（DocumentsProvider）支持**读写 / 新建 / 删除 / 重命名**

### 修复

- 修复 Claude 协议带工具时 **400**（assistant.tool_calls 后必须跟齐 tool 响应）
- 修复 DeepSeek 图片 **422**（`image_url` 字段名序列化错位）
- 修复附件 **LazyRow key 冲突崩溃**（改用 UUID，不再拿 URI 当 key）
- 修复 Response API 路径**思考等级失效**
- 修复**工具审批后 400**（待审批的调用未补齐就续写）
- 修复 **API Key 被清空**（解密失败的空串覆盖了磁盘密文）
- 修复头像被误删（裁剪结果落私有目录，不再走临时路径）
- 修复 **Release 启动崩溃**（R8 裁掉 Ktor 插件类）
- 修复 R8 元数据警告刷屏（AGP 补丁号决定内嵌 R8 版本）
- 修复英语界面显示中文（`values/` 兜底目录被翻译污染）

### 构建

- **APK 体积 15.4 MB → 5.3 MB**（arm64-v8a release；开启 R8 + 资源收缩，tooling 降为 debugOnly）
- AGP `8.13.2`（内嵌 R8 8.13.19）
- Kotlin `2.3.21`
- Ktor `3.4.3`（整族对齐，避免版本撕裂）
- MCP SDK `0.14.0`
