<?xml version="1.0" encoding="utf-8"?>
<resources xmlns:tools="http://schemas.android.com/tools">
    <style name="Theme.MiniiChat" parent="android:Theme.Material.Light.NoActionBar">
        <item name="android:statusBarColor" tools:targetApi="l">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
        <item name="android:windowLightStatusBar" tools:targetApi="m">true</item>
        <item name="android:windowBackground">@android:color/white</item>
    </style>

    <!-- UCrop 裁剪页专用主题：UCropActivity 继承 AppCompatActivity，
         强制要求 AppCompat 系主题（否则运行时抛
         "You need to use a Theme.AppCompat theme"）。
         用 DayNight 自动适配系统深色模式。 -->
    <style name="Theme.MiniiChat.UCrop" parent="Theme.AppCompat.DayNight.NoActionBar" />
</resources>
